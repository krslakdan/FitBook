namespace FitBook.Model.Constants;

public static class PaymentConstants
{
    /// <summary>
    /// ISO 4217 currency code used for all Stripe charges. Lowercase, per Stripe API convention.
    /// </summary>
    public const string Currency = "usd";
}
